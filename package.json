{
  "name": "cognitia",
  "version": "1.0.0",
  "private": true,
  "description": "Cognitia - Discover Grokipedia topics on X.com with AI-powered summaries",
  "scripts": {
    "install:all": "cd extension && npm install && cd ../backend && npm install",
    "build:extension": "cd extension && npm run build",
    "build:backend": "cd backend && npm run build",
    "build": "npm run build:extension && npm run build:backend",
    "dev:backend": "cd backend && npm run dev",
    "start:backend": "cd backend && npm start"
  },
  "keywords": ["chrome-extension", "grokipedia", "x.com", "ai", "grok"],
  "license": "MIT"
}
