{
  "name": "cognitia-extension",
  "version": "1.0.0",
  "description": "Cognitia Chrome Extension - Discover Grokipedia topics on X.com",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "canvas": "^3.2.0"
  }
}
